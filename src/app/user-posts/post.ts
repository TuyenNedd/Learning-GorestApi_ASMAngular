export interface Post {
  id: number;
  user_id: number;
  title: string;
  body: string;
  liked: boolean; // Add the 'liked' property
  commented: boolean; // Add the 'liked' property
  shared: boolean; // Add the 'liked' property

  
}
